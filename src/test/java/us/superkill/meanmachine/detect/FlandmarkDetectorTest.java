package us.superkill.meanmachine.detect;

import org.junit.Before;
import org.junit.Test;

public class FlandmarkDetectorTest {

//  private FlandmarkDetector detector;
//  private Mat image;


  @Before
  public void setup() {
//    ClassLoader classLoader = getClass().getClassLoader();
//    String modelFilePath = classLoader.getResource("flandmark/flandmark_model.dat").getFile();
//    detector = new FlandmarkDetector(modelFilePath);
//    image = imread(classLoader.getResource("obama.jpeg").getFile());
  }

  @Test
  public void detectLandmarksInFace() {
//    Rect face = new Rect(new Point(152, 31), new Point(474, 415));
//    detector.detect(image, face);
  }
}
